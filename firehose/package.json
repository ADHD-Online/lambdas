{
  "name": "firehose",
  "version": "1.0.0",
  "description": "Data firehose from dynamo to bigquery",
  "main": "index.ts",
  "scripts": {
    "build": "tsc && cp -R gcp_keyfile dist/",
    "clean": "tsc --build --clean && rm -r dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.93",
    "@types/node": "^17.0.21",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^5.11.0",
    "zod": "^3.14.4"
  }
}
